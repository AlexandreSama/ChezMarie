<nav class="navbar">
    <div class="logo">
        <a href="{{path('app_home')}}"><i class="fa-solid fa-bread-slice"></i></a>
        <p class="siteName">Chez Marie</p>
    </div>
    <div class="links">
        {% for category in categories %}
        <a href="{{path('app_category', {'id': category.id})}}">{{category.categoryName}}</a>
        {% endfor %}
    </div>
    <div class="user">
        <a href="#"><i class="fa-solid thin fa-cart-shopping"></i></a>
        {% if app.user %}
        <a href="#"><i class="fa-solid thin fa-user"></i></a>
        {% else %}
        <a href="{{path('app_login')}}"><i class="fa-solid thin fa-arrow-right-to-bracket"></i></a>
        {% endif %}
    </div>
</nav>