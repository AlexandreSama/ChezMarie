<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{asset('/img/logo.png')}}" alt="Logo Chez Marie" class="img-fluid" style="max-height: 40px;"> Chez Marie
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-between" id="navbarNavDropdown">
            <div class="navbar-nav">
                <!-- Liens de catégorie -->
                {% for category in categories %}
                <a class="nav-link" href="{{ path('app_category', {'id': category.id}) }}">{{ category.categoryName }}</a>
                {% endfor %}
            </div>

            <div class="d-flex">
                <a href="{{ path('app_basket') }}" class="nav-link">
                    <i class="fa-solid fa-cart-shopping fa-2x me-2"></i>
                </a>
                {% if app.user %}
                    {% if is_granted('ROLE_GERANT') %}
                    <a href="{{ path('app_dashboard_admin') }}" class="nav-link"><i class="fa-solid fa-user fa-2x me-2"></i></a>
                    {% else %}
                    <a href="{{ path('app_dashboard') }}" class="nav-link"><i class="fa-solid fa-user fa-2x me-2"></i></a>
                    {% endif %}
                {% else %}
                <a href="{{ path('app_login') }}" class="nav-link"><i class="fa-solid fa-arrow-right-to-bracket fa-2x"></i></a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>