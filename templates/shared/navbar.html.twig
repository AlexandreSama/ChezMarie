<nav class="navbar">
    <div class="links">
        {% for category in categories %}
        <a href="{{path('app_category', {'id': category.id})}}">{{category.categoryName}}</a>
        {% endfor %}
    </div>
    <div class="user">
        <a href="#"><i class="fa-solid fa-basket-shopping"></i></a>
        {% if app.user %}
        <a href="#"><i class="fa-solid fa-user"></i></a>
        {% else %}
        <a href="{{path('app_login')}}"><i class="fa-solid fa-right-to-bracket"></i></i></a>
        {% endif %}
    </div>
</nav>