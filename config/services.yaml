# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    pdf_directory: '%kernel.project_dir%/public/invoices'
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        
    serializer.normalizer.object:
        class: Symfony\Component\Serializer\Normalizer\ObjectNormalizer
        public: false
        arguments: ['@serializer.mapping.class_metadata_factory', null, null, null, null, null, {'circular_reference_handler': '@app.circular_reference_handler'}]

    app.circular_reference_handler:
        class: App\Handler\CircularReferenceHandler
    App\Service\Basket:
        arguments:
            $requestStack: '@request_stack'
    App\EventSubscriber\OrderEntitySubscriber:
        arguments:
            $SMSState: '@App\Service\SMSState'
            $logger: '@logger'
        tags:
            - { name: 'doctrine.event_subscriber' }
    Symfony\Contracts\Cache\CacheInterface: '@cache.app'
    
    cache.app:
        class: Symfony\Component\Cache\Adapter\FilesystemAdapter
        arguments:
            - 'my_cache_namespace' # Utilisez un nom de namespace approprié
            - 0 # Durée par défaut, 0 signifie que l'élément ne sera pas automatiquement supprimé
        calls:
            - ['setLogger', ['@logger']]

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones